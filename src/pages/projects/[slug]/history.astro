---
import ProjectLayout from '../../../layouts/ProjectLayout.astro';
import HistoryPanel from '../../../islands/HistoryPanel.svelte';

const { slug } = Astro.params;
---

<ProjectLayout slug={slug!} current="history" title="History">
	<HistoryPanel client:load slug={slug!} />
</ProjectLayout>
