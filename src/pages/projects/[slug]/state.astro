---
import GameStateEditor from '../../../islands/GameStateEditor.svelte';
import ProjectLayout from '../../../layouts/ProjectLayout.astro';

const { slug } = Astro.params;
---

<ProjectLayout slug={slug!} current="state" title="State">
	<GameStateEditor client:only="svelte" slug={slug!} />
</ProjectLayout>
