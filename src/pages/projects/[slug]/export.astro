---
import ExportPanel from '../../../islands/ExportPanel.svelte';
import ProjectLayout from '../../../layouts/ProjectLayout.astro';

const { slug } = Astro.params;
---

<ProjectLayout slug={slug!} current="export" title="Export">
	<ExportPanel client:load slug={slug!} />
</ProjectLayout>
