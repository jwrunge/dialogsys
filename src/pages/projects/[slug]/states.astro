---
import ProjectLayout from '../../../layouts/ProjectLayout.astro';
import StateVariableForm from '../../../islands/StateVariableForm.svelte';

const { slug } = Astro.params;
---

<ProjectLayout slug={slug!} current="states" title="States">
	<StateVariableForm client:only="svelte" slug={slug!} />
</ProjectLayout>
