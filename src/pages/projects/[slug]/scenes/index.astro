---
import DialogListManager from '../../../../islands/DialogListManager.svelte';
import ProjectLayout from '../../../../layouts/ProjectLayout.astro';

const { slug } = Astro.params;
---

<ProjectLayout slug={slug!} current="scenes" title="Scenes">
	<DialogListManager client:load slug={slug!} />
</ProjectLayout>
